use std.io
use std.string

fn main() {
    io.println("Hello, Aion!")
    let score = 42
    let bonus = 10
    let total = score + bonus
    io.print("Total Score: ")
    io.println(string.from_int(total))
    return 0
}
