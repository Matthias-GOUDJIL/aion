use std.collections.vector
use std.io
use std.string

fn main() {
    let mut v = Vector<i64>.new()
    v.push(10)
    v.push(20)

    if v.len() == 2 {
        io.println("Simple vector works")
    }
}

